# Mynx 性能基线

在同一台机器、同一构建模式下记录冷启动、空闲 RSS、首个工具进入时间、5 万行矩阵导入、分页/搜索 p95、日志密集运行、10 个搜索标签的 RSS 及关闭后的 RSS。记录 Node、WebView2/WebKit、R 和 OS 版本。

首轮只建立基线，不因单次波动失败。暂定目标：同环境 RSS/冷启动不比基线增加 10% 以上；已解析矩阵分页 p95 小于 100 ms。目标须在三次重复后确认或调整。

建议命名：`benchmarks/<date>-<os>-<build>.json`。不提交包含用户路径、原始数据或个人位置的日志。
