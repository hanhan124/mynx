# Mynx 更新日志

## [2.2.17] - 2026-08-16

### 新增
- 新子功能「RNA-seq 分析」（迁移自 publication_pipeline_wails，侧边栏 / 首页卡片 / 路由自动注册）：
  - **数据导入**：CSV / TSV / TXT / XLSX 计数矩阵，拖拽或粘贴路径导入；矩阵格式预设改为三张引导卡片（标准 Counts / featureCounts / HTSeq），选中即展示数据来源说明、示例表格（自动丢弃的注释列 / `__` 统计行以删除线标示）与自动处理要点，新用户无需文档即可准备数据；featureCounts / HTSeq 预设自动规范化（丢弃注释列、过滤 `__no_feature`、重复基因名求和合并）；样本非零计数质检与分页检索预览。
  - **差异分析**：样本拖拽分组 + 前缀自动分组、Treatment vs Control 比较、可选批次设计（`~ batch + condition`，含混淆拦截）；DESeq2 / edgeR 引擎自动路由，实时 R 日志、取消、失败原因一键复制、历史 run 记录复用。
  - **绘图导出**：14 类图表主从工作台（热图 / PCA / 火山图 / Venn / MA / 箱线 / DEG 柱状 / Top 基因 / 树状图 / 富集 / GSEA / 小提琴 / 密度），快速预览（低分辨率 PNG）与单图 / 批量导出，Nature / Science / Cell 期刊尺寸预设、ggplot 主题与调色板、结果画廊内嵌预览。
  - 进入该功能窗口自动最大化（保留任务栏的标准最大化，非全屏），离开时恢复进入前的尺寸与位置（进入前已最大化则保持不变）；进入时内容先隐藏、窗口最大化落定后统一淡入，窗口动画与内容渲染分离不互相拖帧；UI 完全沿用 Mynx 设计体系，并内置使用教程。
  - 以「测试版」标识：首页卡片 Beta 角标、侧边栏图标小圆点、页头测试版标签与可关闭的提示条（说明功能仍在开发完善中、提醒备份重要数据）。
  - 统计计算由外部 R 引擎完成（runner.R 随应用打包），需安装 R ≥ 4.0；输出 Excel 总表 / CSV / PNG·SVG·PDF 图表 / Analysis_Report.md。
  - 双平台（Windows / macOS）：路径读取超出 fs 作用域时按系统回退（PowerShell / bash+BSD 工具），Rscript 自动定位（PATH / 注册表 / Homebrew / R.framework），取消分析按平台终止进程树；实机验证 Windows 全链路（导入 → DEG → 绘图 → 历史复用）。

## [2.2.16] - 2026-08-11

### 优化
- qPCR 计算「重复次数」上限从 10 提升到 12，支持最多 12 个技术重复。

## [2.2.15] - 2026-08-09

### 新增
- qPCR 计算新增「重复处理」下拉框，两种模式二选一：
  - **择优重复数**：为每个样本挑选标准差最低的 K 个重复，生成 `Summary_Best_Replicates` 汇总表（仅作内部 QC 参考，不直接用于发表）。
  - **离群值剔除**：迭代剔除偏离组均值超过 K×SD 的重复，生成 `Summary_Outlier_Removed` 汇总表（规则客观，可在方法学中披露）。

### 优化
- 检测到新版本时，更新提示框直接显示本次更新的摘要说明。
